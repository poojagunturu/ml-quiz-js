# This or That Quiz App

This is a 'This or That' quiz app that uses a computer vision model to detect the users head tilt direction and selects the option on the left or right based on the direction of tilt. This app is built using [Google's Teachable Machin](https://teachablemachine.withgoogle.com/) and [TensorFlow.js](https://www.tensorflow.org/js). 